{
  "name": "k9",
  "private": true,
  "scripts": {
    "check": "turbo run //#lint types //#format --continue",
    "format": "oxfmt --check",
    "format:fix": "oxfmt",
    "lint": "oxlint --type-aware",
    "lint:fix": "oxlint --type-aware --fix-dangerously",
    "types": "turbo run types",
    "dotdev#deploy:prod": "pnpm run build --filter=dotdev && pnpm run deploy:prod --filter=dotdev",
    "dotdev#dev": "turbo run dev types:watch lint:watch format:watch --filter=dotdev",
    "dotrun#dev": "turbo run dev:vite dev:convex types:watch lint:watch format:watch --filter=dotrun",
    "sandbox#dev": "turbo run dev types:watch lint:watch format:watch --filter=sandbox"
  },
  "devDependencies": {
    "oxfmt": "0.22.0",
    "oxlint": "1.37.0",
    "oxlint-tsgolint": "0.10.1",
    "turbo": "2.7.3"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@10.27.0"
}
