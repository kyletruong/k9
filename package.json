{
  "name": "k9",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev:www": "turbo run dev:vite dev:convex --filter=www",
    "lint": "biome check",
    "lint:fix": "biome check --write",
    "lint:fix-unsafe": "biome check --write --unsafe",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.3",
    "turbo": "2.6.0"
  },
  "packageManager": "pnpm@10.20.0",
  "engines": {
    "node": ">=24"
  }
}
